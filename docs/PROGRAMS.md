# Programs

LunarOps intentionally exposes six programs:

| Program | Purpose |
|---|---|
| `NormalPointsConvert` | Convert external MINI or CRD/FRD data to the canonical normal-point file. |
| `StationCatalogCreate` | Create an ITRF station catalog from a coordinate source file or inline coordinates and rates. |
| `ReflectorCatalogCreate` | Create a Moon-PA reflector catalog from a coordinate source file or inline XYZ. |
| `LlrResiduals` | Evaluate O-C residuals without estimating parameters. |
| `LlrProcessing` | Run nonlinear estimation and write the requested final products. |
| `LlrObservationPrediction` | Evaluate a UTC time grid for LLR pointing and coarse visibility windows. |

```bash
python -m lunarops list-programs
python -m lunarops describe-program LlrProcessing
python -m lunarops validate config.yml
python -m lunarops run config.yml
python -m lunarops run config.yml --mpi
```

`--set name=value` overrides entries in `variables`. Validation checks the
schema, artifact graph, paths, and class choices without evaluating the LLR
model.

## Residual workflow

```yaml
programs:
  - program: NormalPointsConvert
    inputFilesNormalPoints: [data/input.crd]
    outputFileNormalPoints: output/normalPoints.txt.gz
    outputFileImportReport: output/importReport.txt.gz

  - program: LlrResiduals
    inputFilesNormalPoints: [output/normalPoints.txt.gz]
    inputFileStationCatalog: output/stations.txt
    inputFileReflectorCatalog: output/reflectors.txt
    outputLevel: standard
    outputFileObservationResults: output/oc.txt.gz
```

## Processing workflow

`LlrProcessing` keeps estimation stages and output selection in one ordered
`processingSteps` list. Selection and estimate steps must precede output
steps. Exactly one leading `screenObservations`, at least one `estimate`, and
at least one output step are required.

```yaml
programs:
  - program: LlrProcessing
    inputFilesNormalPoints: [output/normalPoints.txt.gz]
    inputFileStationCatalog: input/stations.txt
    inputFileReflectorCatalog: input/reflectors.txt
    varianceComponents:
      - {id: all, station: STA, start: 2000-01-01, endExclusive: null}
    processingSteps:
      - type: screenObservations
        residual:
          maximumAbsoluteM: 2.0
        reportedSigma:
          minimumOneWayM: 1.0e-3
          minimumFractionOfGroupMedian: 0.1
      - type: selectParametrizations
        parametrizations: [reflectorPosition, stationRangeBias]
      - type: estimate
        name: joint
        maxIterationCount: 10
        estimateVarianceFactors: true
        estimateRobustWeights: true
        robustWeighting: {model: igg3, k0: 1.5, k1: 6.0}
      - type: writeResiduals
        outputFile: output/postfit.txt.gz
        outputLevel: full
      - type: writeNormalEquations
        outputFile: output/normals
      - type: writeResults
        outputFileReport: output/processingReport.txt
        outputFileState: output/processingState.txt
        outputFileSolution: output/solution.txt
        outputFileCovariance: output/covariance
        outputFileReflectorCatalog: output/reflectors.txt
```

The full accepted model and estimation fields are generated by
`describe-program`; the registry is authoritative.

## Observation prediction

`LlrObservationPrediction` reuses the complete two-way light-time model for a
selected station and reflector. For each UTC grid epoch it writes the uplink
event pair (`t1`, `t2`), geometric azimuth/elevation, the complete ENU line-of-sight
unit vector, Sun elevation, the Apollo mean elongation `D` from ERFA `fad03`, and an observable flag. Adjacent
observable grid samples are also written as coarse visibility windows. The
current program deliberately does not include CPF comparison, atmospheric
refraction, pointing-model corrections, or hardware-control output.

```yaml
programs:
  - program: LlrObservationPrediction
    inputFileStationCatalog: output/stations.txt
    inputFileReflectorCatalog: output/reflectors.txt
    outputFilePrediction: output/llr_prediction.txt
    outputFileWindows: output/llr_prediction_windows.txt
    startTime: "2024-09-18T00:00:00"
    endTime: "2024-09-18T23:55:00"
    stepSeconds: 300
    stationName: APOLLO
    reflectorName: APOLLO15
    minElevationDeg: 20
    minReflectorElevationDeg: 0
    maxSunElevationDeg: -6
    allowedElongationRangesDeg: [{startDeg: 30, endDeg: 330}]
```

The complete runnable example is `configs/lunarops_observation_prediction.yml`.

## Reflector catalog creation

`StationCatalogCreate` and `ReflectorCatalogCreate` keep coordinate sources
separate from the observation programs. Each accepts either an external YAML
coordinate source file or an inline sequence (`stationCoordinates` contains
key, XYZ, and optional rate/epoch; `reflectorCoordinates` contains only key and
XYZ), and writes a typed catalog file for later program input.
